1）一行文字有多种属性，怎么方便的设置
    方法一：如果是多行代码，可以使用<pre>标签，<pre> 标签的主要作用:预格式化的文本,被包围在 pre 元素中的文本通常会保留空格和换行符。（各种样式需要不同的标签辅助）
    方法二：前后after before 设置（多个不太合适）
    方法三：可以直接用多个空格 &nbsp;在html中直接排版（html中内容复杂）
2）图标的颜色怎么改变，渐变色怎么设置，图标的不规则边框颜色怎么设置（如果直接对i设置时都会影响标签以外的白色区域）
3）文字的渐变色除了可以用html5中fillText还有另外的方法吗？

